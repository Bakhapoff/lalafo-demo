package com.bektur.bakhapov.lalafo.payload.response.ad;

import java.util.List;

public record AdResponse(
        Long id,
        Long old_id,
        Long country_id,
        Long status_id,
        Long category_id,
        Long user_id,
        List<Long> user_ids,
        AdUserResponse user,
        Long origin_user_id,
        String title,
        String description,
        String city,
        Boolean hide_phone,
        Boolean hide_chat,
        Double lat,
        Double lng,
        Integer views,
        Integer impressions,
        Integer favorite_count,
        Integer callers_count,
        Integer writers_count,
        Boolean is_vip,
        Boolean is_select,
        Boolean is_premium,
        Boolean is_negotiable,
        Double price,
        Double old_price,
        String currency,
        String symbol,
        String background,
        String mobile,
        List<AdImageResponse> images,
        Long created_time,
        Long updated_time,
        Long city_id,
        String city_alias,
        Boolean can_free_push,
        List<String> paid_features,
        List<String> paid_packages,
        Long score_order,
        Long last_push_up,
        List<AdTrackingInfoResponse> tracking_info,
        Boolean is_freedom,
        Integer response_type,
        Boolean campaign_show,
        Boolean is_ppv,
        Integer decoration_mask,
        Integer price_type,
        String ad_label,
        List<AdParamResponse> params,
        Integer national_price,
        Integer national_old_price,
        Boolean is_identity,
        String url
) {
}